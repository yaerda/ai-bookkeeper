<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION"
        tools:targetApi="34" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"
        tools:targetApi="34" />

    <!-- Allow querying speech recognition services (Android 11+ package visibility) -->
    <queries>
        <intent>
            <action android:name="android.speech.action.RECOGNIZE_SPEECH" />
        </intent>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <package android:name="com.google.android.googlequicksearchbox" />
        <package android:name="com.iflytek.speechsuite" />
        <package android:name="com.baidu.input" />
        <package android:name="com.baidu.input_oppo" />
        <package android:name="com.sohu.inputmethod.sogouoem" />
        <package android:name="com.samsung.android.bixby.agent" />
        <package android:name="com.heytap.speechassist" />
        <package android:name="com.huawei.vassistant" />
    </queries>

    <application
        android:name=".AiBookkeeperApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AiBookkeeper">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AiBookkeeper.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Transparent Activity for quick input from notification -->
        <activity
            android:name=".QuickInputActivity"
            android:exported="true"
            android:theme="@style/Theme.AiBookkeeper.Transparent"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="com.aibookkeeper.action.QUICK_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.aibookkeeper.action.QUICK_VOICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.aibookkeeper.action.QUICK_CAMERA" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.aibookkeeper.action.QUICK_CATEGORY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.aibookkeeper.feature.capture.screenshot.ScreenshotCaptureActivity"
            android:exported="false"
            android:theme="@style/Theme.AiBookkeeper.Transparent"
            android:excludeFromRecents="true"
            android:launchMode="singleTask" />

        <!-- Undo transaction from success-feedback notification -->
        <receiver
            android:name="com.aibookkeeper.feature.capture.notification.UndoTransactionBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.aibookkeeper.action.UNDO_TRANSACTION" />
            </intent-filter>
        </receiver>

        <!-- Persistent notification foreground service -->
        <service
            android:name="com.aibookkeeper.feature.capture.notification.PaymentNotificationService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection|specialUse" />

        <!-- Payment notification listener (auto-capture from WeChat/Alipay/etc.) -->
        <service
            android:name="com.aibookkeeper.feature.capture.notification.PaymentNotificationListener"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name="com.aibookkeeper.feature.capture.accessibility.PaymentAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
