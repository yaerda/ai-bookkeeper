<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.AiBookkeeper" parent="android:Theme.Material.Light.NoActionBar" />

    <color name="splash_bg">#FFF5F0E8</color>

    <!-- Splash screen theme — transparent icon, matching background color -->
    <style name="Theme.AiBookkeeper.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_transparent</item>
        <item name="windowSplashScreenAnimationDuration">0</item>
        <item name="postSplashScreenTheme">@style/Theme.AiBookkeeper</item>
    </style>

    <!-- Transparent theme for QuickInputActivity -->
    <style name="Theme.AiBookkeeper.Transparent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
    </style>
</resources>
