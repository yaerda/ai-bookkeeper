package com.aibookkeeper.core.data.local.converter

/**
 * Room TypeConverters.
 *
 * Design note: All enums are stored as String and timestamps as Long (epoch millis),
 * so no custom TypeConverters are needed for v1.0.
 * Add converters here when complex types are introduced.
 */
class Converters
