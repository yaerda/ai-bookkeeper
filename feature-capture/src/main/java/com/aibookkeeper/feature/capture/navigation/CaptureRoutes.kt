package com.aibookkeeper.feature.capture.navigation

object CaptureRoutes {
    const val CAMERA = "capture/camera"
    const val CAMERA_WITH_ACTION = "capture/camera?action={action}"
    const val OCR_RESULT = "capture/ocr_result"
}
